---
// FranjaEquipo.astro — franja de confianza que abre «Gestión de equipo».
// Peso visual BAJO a propósito: una línea, cuatro caras y un enlace. No
// compite con el hero ni con las secciones de módulo — su trabajo es que la
// cifra tenga cara antes de que el visitante decida si nos cree.
//
// El titular decía «un equipo que responde por ella» y John lo descartó: leía
// como garantía contractual sobre cada dato. La versión final se compromete
// con la CALIDAD DEL DATO —un estándar de trabajo, no una promesa de
// resultado—, que es exactamente lo que el resto del sitio ya cumple: cada
// cifra declara su muestra, y si el dato no existe, se dice.
//
// Las fotos son las mismas de /nosotros (public/equipo/).
import Flecha from "./Flecha.astro";
const EQUIPO = [
  { foto: "/equipo/john-chamorro.webp", nombre: "John Chamorro" },
  { foto: "/equipo/jairo-marin.webp", nombre: "Jairo Marín" },
  { foto: "/equipo/alejandra-viveros.webp", nombre: "Alejandra Viveros" },
  { foto: "/equipo/jaderd-dorado.webp", nombre: "Jaderd Dorado" },
];
---

{/* Visibilidad, segunda pasada (2-sep). La primera subida se pasó: John pidió
    la MITAD EXACTA entre los dos tamaños —avatar 40→56 queda en 48, titular
    15→20 en 17,5, subtexto 13→15 en 14, aire 32→40 en 36— y el fondo de
    vuelta a blanco. De aquella pasada se queda lo que hace el trabajo con
    menos ruido: el filete azul arriba y abajo, y el CTA como botón de borde.
    Sigue siendo franja, no sección. */}
<section class="border-y border-azul-500/20 bg-white">
  <div class="mx-auto flex max-w-contenido flex-wrap items-center gap-x-9 gap-y-[1.125rem] px-5 py-9 sm:px-8">
    <div class="flex -space-x-[0.6875rem]" aria-hidden="true">
      {EQUIPO.map((m) => (
        <img
          src={m.foto}
          alt=""
          width="80"
          height="80"
          loading="lazy"
          decoding="async"
          class="h-12 w-12 rounded-full border-2 border-white object-cover"
        />
      ))}
    </div>
    <div class="min-w-0 flex-1">
      <p class="text-[1.09375rem] font-bold leading-snug tracking-tight text-grafito-900">
        Detrás de cada cifra hay un equipo comprometido con la calidad del dato.
      </p>
      <p class="mt-1 text-[0.875rem] text-grafito-500">
        Ingeniería, análisis de datos y experiencia en contratación pública.
      </p>
    </div>
    <a
      href="/nosotros/"
      class="inline-flex min-h-[44px] items-center whitespace-nowrap rounded-lg border border-azul-500/40 bg-white px-5 text-sm font-semibold text-azul-600 transition hover:border-azul-500 hover:text-azul-700"
    >
      Conoce al equipo <Flecha />
    </a>
  </div>
</section>
